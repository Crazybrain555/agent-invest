# L1 来源资产层整体规划 v0.2(重写稿)

状态:2026-07-06 依据用户反馈重写,替代 v0.1(文件已随版本改名,原 `..._v0.1.md` 不再保留)。
**待用户通读并手动修改;之后的执行以用户改定稿为准。**
本文只管 L1 层"应该做成什么样":定位、各路径职责、横切纪律、缺口。**不做里程碑排程**——
怎么分步,由用户在改定稿后决定。
provider 接入的技术定稿在 `asset_intake_provider_framework_v1.2.md`(F1–F12 + 实测附录),
本文引用不复述。语义权威:协议 v0.7(重点 §2 统一范式、§3 L1、§15 工程取舍、§16 硬边界)。
事实纪律沿 v1.2:凡未实测/未核实的条目显式标注"未实测",不当作已知。

## 0. 相对 v0.1 的三处方向修正(用户反馈)

1. **L1 自身的筛选/处理/清洗职责写全**。v0.1 重心在"接什么源",对 L1 的加工职责
   (载体规范化、质量控制、查空、幂等取代、scope 继承)着墨不足,并错误地把 upload_service
   挂在"L2 最小闭环之后"。按协议 §15.1:raw 处理职责全部在 L1,不因存储"轻"而下放;
   L2 缺位只影响证据化,不影响 L1 资产沉淀。L1 的职责不拿 L2 进度当前置。
2. **结构化数据路径去死板**。v0.1 只有"预注册 dataset_key 才可取数"一条道。本版增加
   "数据字典可见 + 受控 ad-hoc 取数 + 复用即转正"(第 3 章),让 AI 在预注册数据集之外
   也有合规取数途径,承接协议 §8.6 / §10.2 的拉式需求。
3. **删除排程内容**。v0.1 的"建议后续里程碑"与各处"建议时机"全部删除;只保留缺口事实
   (第 6 章)与真正的设计层开放问题(第 7 章)。

## 1. L1 的定位与边界(协议 §3.1 / §3.4 / §3.5)

L1 回答:这条资料/数据/工具结果来自哪里?什么时候、用什么 adapter / query / parser 取得?
现在是什么可消费形态?追溯等级、质量状态和下游契约是什么?

L1 不回答:这是不是高价值投资信息?支持哪个假设?与历史证据是否冲突?应不应该采信?

**L1 有自己的"筛选处理清洗",与 L2 的筛选是两段不同的活:**

```text
L1 载体规范化(carrier normalization,§3.5)——处理结构与载体,不碰投资语义:
  解析(PDF / Word / PPT / 图片 OCR / 录音转写 / Excel)
  → 按业务结构切分(段落 / 表格 / Q&A;不按版面和 token 切)
  → 表头 / 单位 / 脚注保留,Q&A 问答绑定
  → heading_path / order_index / semantic_key / content_hash 生成
  → 稳定噪声处理(页眉页脚 / 页码 / 水印 / 控制字符)
  → raw 保真留存 + quality_status 标注(ok / needs_review / unusable / empty)
  结构化数据侧的同类活:字段契约核实(列名 / 单位 / 活表裁决)、类型规整、
  查空登记、幂等去重与取代——同样是"清洗",只是对象是 recordset 不是版面。

L2 证据化筛选(§4.5)——高价值选择性抽取、registry 归一、对账、冲突、采信。
```

一句话判别:**L1 负责让资产不孤立、可追溯、可消费;L2 负责理解资产在说什么。**

安全红线(§3.5):L1 不得因"像套话"删除可能含实质风险、业绩变化、会计政策、重大事项的
信息——可以标质量、可以不生成 unit,但原文与 parser artifact 必须保留、可重处理,
不得让下游永久不可见。

边界三条(§16.3 / 7 / 9):raw 止于 L1;"先轻后重"只指存储不指职责——解析 / OCR /
表格识别 / 上传件处理全是 L1 的活;L1 资产暂无消费方(L2 未立项)是协议分期常态,
资产照常沉淀。

## 2. 全景:四条路径 × 一个共享核

```text
路径          服务/入口                          状态            统一出口(data_asset)
披露 PDF      services/disclosure_anchor         live(07 进行)   document_unit
结构化数据    services/asset_intake              M-C 完成        dataset_snapshot
工具/网页     asset_intake.register_tool_result  入口就绪,源未接 tool_result
人工上传      upload adapter                     未立项          document_unit /
                                                                 dataset_snapshot /
                                                                 artifact_unit
共享核        packages/envelope_kernel           M-B 完成        信封模型 / kind 矩阵 /
                                                                 asset:// URI
```

### 2.1 披露 PDF:disclosure_anchor(L1 模板实现,§3.10)

现状:milestone 06 Filing API 冻结(送审通过);07 CNINFO 增量同步由并行会话进行中
(免凭证 web fallback、real-API 验收 A25/A26/A27 已过)。本会话对该子树只读。
其手法(三 schema、public views `*_v1`、outbox、稳定键列化 + jsonb payload、
processing_run 版本管理)是其余路径的复用蓝图。

缺口(对照 §3.10 三点补强,不排时序):

- **G1 change feed 端点**:outbox_event 表 + change_events_v1 视图已落;`GET /v1/changes`
  端点与 observed / materialized 两事件语义(§2.8)未落。
- **G2 envelope_kernel 反向接入**:术语已收敛(asset_id / payload_kind / event_kind),
  但信封模型、URI 规则、kind 矩阵仍是服务内自持定义;目标是用共享包契约测试替换服务内
  重复定义,不改运行时行为。
- **G3 跨路径对账锚**:(security, report_period) 键两侧已就绪(披露侧 filing、intake 侧
  fin_statement / earnings_event)。对账本身是 L2 职责,L1 只保证键可用——已达标,持续维持。

### 2.2 结构化数据:asset_intake

现状(M-C,P1–P6,commit 链见其 docs/agent):端口冻结、双层配置(4 dataset +
2 provider catalog,全部实测核实)、确定性 registrar(幂等三态 + supersede)、
intake_public 三视图 + 权限收口、契约导出、tool_result 登记入口;真实 F12 preflight
对活 RDS 全绿。

缺口:真实 provider adapter 未接(WindRdsProvider / TushareApiProvider,含 freshness
smoke 与显式 opt-in 真源测试);source_checkpoint 增量游标未建;**只有"预注册 dataset"
一种使用形态——第 3 章展开修正**。

### 2.3 轻载工具结果:tool_result 路径

现状:`register_tool_result` 入口就绪(每条 item 必带 locator、错误≠查空、Tier 2 / G2 默认)。

职责与缺口:

- **源目录化**:web search / MCP / API 源各建 `registry/providers/*.catalog.yaml` 同款配置
  (F12 同一原则:一切皆配置、预检 fail-fast);
- 真实源尚未接入;候选源事实与选型判据见附录 A(不排批次);
- 纪律(§4.7 / §16.18):**agent 总结不是来源**,其引用的网页 / API 返回 / 原文片段才是;
  结果进系统必须显式过 `register_tool_result`;弱信源(传闻 / 情绪类)预设 Tier 3。

### 2.4 人工上传:upload adapter

职责(§3.8;全部是纯 L1 的活,不依赖 L2 存在):

- **登记与哈希**:原件保真留存 + raw_asset_ref(路径 + 哈希);
- **载体规范化**:纪要文本切分、Excel 区块结构化、图片 OCR、录音转写——上传路径是 L1 里
  "清洗"最重的一条,这正是它值得独立立项的原因,而不是"等 L2 才有价值"的理由;
- **material_type 由人显式声明**,L1 不做语义推断;按内容形态分发 document_unit(纪要文本)/
  dataset_snapshot(数据摘录)/ artifact_unit(计算 / 模型工件);拿不准默认 artifact_unit
  登记(之后 L2 走 change_candidate 复核是 L2 的事,L1 只管登记完整);
- **sensitivity 标记**(§3.2 D 组):一手资料 / 未公开重大信息的合规隔离字段,登记时落列;
- **时间语义**:event_time 用资料对应时间而非上传时间;published_at 按 source_available_at
  语义解释(§2.5),无法证明更早可用时退化到 observed_at。

形态(独立服务 vs 并入 asset_intake 第三入口)是开放问题(第 7 章);立项与排期由用户定,
本文不设任何前置条件。

### 2.5 共享核:envelope_kernel

信封模型、asset_kind × payload_kind 矩阵、asset:// URI 已抽共享包(M-B)。持续义务:
四条路径的新增字段一律先查协议 §3.2 六组与扩展字段纪律(§2.1),默认不新增顶层对象;
kind 矩阵变化经协议升版,不在服务内私改。

## 3. 结构化数据路径:从"预注册"到"字典可见、受控取数"

v0.1 的隐含假设是"所有取数需求都能预先注册成 dataset"。这对订阅型主链(行情 / 财务 /
事件)成立,对 AI 的探索型需求不成立——课题、evidence_request、人工问题随时会要
"字典里有但没注册"的数据。修正为三条使用形态(Lane),同一账本、同一信封出口:

### 3.1 Lane A:注册数据集(现状,主链)

适用:长期订阅、增量同步、将进 L2 numeric_observation 的数据(行情 / 三大表 / 指标 / 事件)。
纪律维持现状:dataset_key 语义契约(registry/datasets)+ provider catalog 物理契约
(registry/providers)+ registrar 幂等登记;每字段配 mapping / unit / fixture。

### 3.2 Lane B:数据字典是一等对外资源(§15.5 数据目录的延伸)

字典分两件,角色不同(2026-07-06 核对仓库实况后的准确表述):

- **契约件(已存在)**:`registry/providers/*.catalog.yaml`(wind 8 个已消费 table_alias +
  tushare 8 个 endpoint,含安全默认、单位注记、活表裁决)与 `registry/datasets/*.yaml`
  (4 条语义契约,列 / 单位 / fixture)。机器可读、Git 管理、实测核实;按 v1.2 §9 定案
  **有意不收未消费表**(防幽灵条目)。
- **库存件(不存在——Lane B 的真正新建物)**:全库 inventory。RDS 50 表的表 / 列清单目前
  只在 v1.2 §11 的文字附录里,不可机读。inventory 应由 prober 现场探测生成(日期戳快照;
  活库是唯一权威,v1.2 §9 权威链),是探测产物不是契约,因此不违反"不登记幽灵条目"
  ——不进 catalog 正文,独立成件。

让 AI 看得到的形态:

- **近期**:一条 CLI 导出两件——契约件(export_contracts 已有,扩展)+ inventory 探测件
  (表 / 列清单、活表状态、新鲜度探测结论);
- **目标**(§3.11 MCP 映射):字典条目 = `resources/list`,内容 = `resources/read`,只读;
  写动作(登记、取数)一律建模为 tool;
- **纪律**:字典只描述"有什么、什么口径、怎么定位",不含凭证;契约件(实测核实)与
  inventory 探测件(存在性已探、语义未核)在字典里可区分——AI 用到"仅 inventory 可见"
  的表 / 列时,自己能看见"语义未核"的风险。

### 3.3 Lane C:受控 ad-hoc 取数

AI(evidence_request 执行者 / 课题 / 人工指令)对"字典里有但没注册"的需求,允许在受控回路
内自主取数。协议依据:§8.6(evidence_request)、§10.2(L6 或人工发起数据需求 → L1 provider
adapter)、§6.5.1 规则 5(确需在线拉取走受控回路,产物仍先落 L1)。

**形态遵守协议原则 11"LLM 出候选,规则做裁决"与 v1.2 F7 / F8**——AI 不在运行时写自由 SQL,
而是照着字典起草一份临时查询配置(候选),交确定性校验器裁决后执行:

```text
入口:evidence_request 或人工指令(source_policy 圈定 provider,budget 限轮次 / 费用)
起草:AI 读字典(Lane B)→ 起草临时查询模板(表 / 列 / 谓词 / 绑定参数——与 dataset YAML
      的 providers 段同构的轻量配置,不是自由 SQL 文本)
裁决:确定性 validator 按 F7 全套安全规则校验(白名单表列、绑定参数、required predicates、
      max_rows、timeout、deny tokens、禁 SELECT *、只读账号、varchar8 日期列禁 CONVERT)
      + F12 预检 fail-fast;不过即拒,报错列明缺什么
执行:经 asset_intake 的 adapter / CLI(未来 MCP tool),不是让 agent 拿凭证裸连
      ——查空、登记、审计自动发生;临时裸连只读库属调试行为,不产生可引用资产,
      任何要进系统的结果必须经登记入口
登记:结果照常登记 data_asset(asset_kind = dataset_snapshot, payload_kind = recordset),
      dataset_key 用保留命名空间 adhoc.<provider>.<slug>;provider / query_params /
      returned_fields / provider 侧 as-of / adapter 版本必填(§3.7)——可复查是底线;
      查空照记 source_access(§3.9);访问动作落 action_log
消费:adhoc 快照没有预注册字段契约,这是显式状态(不是缺陷伪装成正常);
      L2 消费前需字段映射确认——那是 L2 准入的事,L1 侧只保证登记完整
```

与 v1.2 的关系:F8 已为"自由问数"定向 text-to-DatasetRequest(候选经 registry validator
确定性裁决,**不是 text-to-SQL**),但只覆盖已注册 dataset;Lane C 是同一原则向"字典内
未注册"的推广——落地前需给 v1.2 增补一条"临时查询模板快速通道"条款(F7 安全规则全套
沿用、F8 原则不破,属增补而非改判;改 v1.2 需按其头部要求说明与协议 §2.1 / §16 的一致性)。

禁止的仍然禁止(§16.24 / §16.31):不登记的旁路取数、拿 agent 总结当来源、绕过 L1 / L2
改预测。Lane C 不是放开自由取数,是把"AI 有别的需求"收进受控回路。

### 3.4 晋升规则:复用即转正

need-to-register 纪律从"先注册才可用"调整为:**探索可先走 Lane C,复用需求转正为 Lane A**。
转正判据(满足其一):同一 adhoc 查询模式重复出现;需求进入 L5 数据订阅(§7.6);
需要 source_checkpoint 增量维护。转正动作 = 建 dataset yaml + catalog 补实测 + fixture;
历史 adhoc 快照不迁移,保持原样可追溯。

### 3.5 provider 通道事实(2026-07-06 实测,详见 v1.2 §11)

- **wind_rds**(阿里云 SQL Server,50 表子集):主通道。行情至上一交易日、预告至当天;
  批量 / 表宽占优;活表裁决与列名 / 单位全部实测入 catalog。
- **tushare_api**(HTTP,同事共享 token,将换密钥):备用 + 交叉校验。八个所需 endpoint
  当前 token 全通;限流按积分。

| 维度 | wind_rds(实测) | tushare_api(实测) | 同花顺 iFinD(候选,**未实测**) |
|---|---|---|---|
| 覆盖 | A股行情/财务/事件/指数/基金/HK 50 表 | 覆盖同类,接口粒度细 | 宣称全,未核 |
| 新鲜度 | 预告当天、行情 T-1 | 同级 | 未核 |
| 稳定性 | RDS 直连,受同事同步作业影响 | 官方 SaaS,受积分/频控 | 未核 |
| 限流 | 无(自律:白名单模板+行数上限) | 积分制,min_interval 300ms | 未核 |
| 凭证合规 | 同事账号,将轮换;只读 | 同事 token,将轮换 | 需自有账号,采购流程 |
| 接入成本 | catalog 已就绪,只差 adapter | 同左 | 全新 catalog + adapter |
| dataset_key 映射 | 四条首发全覆盖 | 四条首发全覆盖 | 待字典核实 |

结论维持 F11:wind 主、tushare 备;iFinD 仅在前两者不可持续时启动评估,不预投入。
同事的 tushare MCP 封装不作为接入通道(账本唯一:source_access + data_asset,不允许第二套
SQLite cache);其限流退避、错误分类、工具索引设计已吸收为 catalog 与 adapter 实现参考。

### 3.6 dataset 扩容候选(不排序;启用前按 v1.2 §9 权威链逐列实测:活库 / 活 API 探测 > 官方字典 > 任何评审或记忆)

- `cn_equity.valuation_daily`:估值日频(RDS 实测有 AShareValuationIndicator 表 +
  AShareEODDerivativeIndicator 视图;列 / 单位未核);
- 证券主档 / 行业分类(AShareDescription / AShareIndustriesClassCITICS 等,实测存在):
  喂将来的 Subject Registry(L4 / L6 议题,不是 L1 dataset 常规扩容);
- 披露日历(财报季调度用):RDS **无** AShareIssuingDatePredict;tushare `disclosure_date`
  接口**未实测权限**——启用前先核;
- 一致预期(AShareForcast,实测存在):协议 §15.6 baseline claims 暂不启用,仅登记为
  将来对照模块的输入,不进 L5 主假设。

### 3.7 增量与调度能力

- 行情:AShareCalendar(实测至 20271231)驱动 security × trade_date 窗口;
- 财务 / 事件:公告日(ANN_DT / ann_date)增量 + lookback 窗口兜重述,不按 max(report_period);
- 两者的载体是 **source_checkpoint 增量游标表**(新 migration);
- 调度形态遵守 §15.7:定时批 + 手动 CLI 拉起,不依赖实时在线;disclosure_anchor 的
  change feed(G1 落地后)触发同期财务拉取,是跨服务联动的目标形态。

## 4. 横切职责(四条路径统一的 L1 全域纪律)

1. **登记与追溯**:source_ref 是 provenance 引用,可由视图 / endpoint 派生(§2.3);
   每次访问(含查空)落 source_access,错误≠查空(§3.9);凭证只走环境变量。
2. **幂等 / 版本 / 取代**:content_hash 判变;新版本取代不覆盖(superseded_by);
   append-only 是全系统一跳引用成立的前提(§12.3)。
3. **scope 继承承诺**(§3.6 / §16.10):public view 与 API 返回的每个 unit / snapshot 带全
   scope keys(company_ref / security_ref / report_period / material_type…);物理存储可范式化。
4. **Subject Registry 采集义务**(§6.5.1 规则 4 / 5):所有创建公司 / 证券侧信息的 L1
   producer 对 identifier scheme(lei / uscc / sec_cik / isin / exchange:ticker…)
   "有则必填";强键校验数据走本地快照,不在主链路实时调外部 identifier API。
   现状:披露侧已有 company_identifier 表(scheme 覆盖**未核**);intake 侧
   subject_candidates 是 subject_key 形态、尚无 identifier 采集。盘点列入缺口。
5. **change feed**(§2.8):每个 L1 服务对下游暴露同构 feed——outbox + change_events_v1
   视图 + `GET /v1/changes?after_seq` 目标端点;observed / materialized 两事件语义,
   下游失效只由 materialized 触发。disclosure_anchor 与 asset_intake 都需补齐端点与两语义。
6. **对外契约五件套**(§3.11):可寻址(asset:// URI)、读契约版本化(`*_v1`)、可检索
   (scope keys 过滤 + 游标分页)、change feed、错误模型(L1_PROCESSING_REQUIRED /
   NOT_FOUND / CONTRACT_VERSION_MISMATCH / GONE_SUPERSEDED)。MCP 映射设计上零改造:
   查询 = tool,取回 = resource,变更 = notifications;资源只读,写走 tool。
   现状:读侧暂 SQL 视图,HTTP / MCP 未建(已知偏差,显式记录)。
7. **数据目录**(§15.5):检索键 / URI / public views 随服务落地(已做);
   字典暴露(§3.2 Lane B)是它在结构化路径上的延伸。

## 5. 协议对齐检查(§3 / §15 / §16)

| 条目 | 状态 |
|---|---|
| §3.2 data_asset 信封 | ✓ envelope_kernel + intake_core.data_asset;披露侧待反向接入(G2) |
| §3.3 四 asset_kind | document_unit ✓ / dataset_snapshot ✓ / tool_result ✓(入口)/ artifact_unit 待 upload adapter |
| §3.4 raw 止于 L1 | ✓ 两服务均不外溢 raw;上传件 OCR / 转写 / Excel 结构化职责归 L1(§2.4) |
| §3.5 载体规范化 | 披露侧 ✓;结构化侧为字段契约 / 类型规整形态 ✓;上传侧待建 |
| §3.6 scope 继承 | ✓ 两侧 public views 均带全 scope keys |
| §3.7 payload 契约 | ✓ dataset_registry 字典(Git)+ 契约测试;tool_result 必填项硬校验;adhoc 快照"无预注册契约"为显式状态(§3.3) |
| §3.8 人工上传 | 职责已定义(§2.4),服务未立项 |
| §3.9 查空 | ✓ source_access(empty),不建资产;Lane C 沿用同一机制 |
| §3.10 蓝图复用 | ✓ intake 全套照蓝图;信封核已抽共享包 |
| §3.11 对外契约 | 视图 + 错误码 + URI ✓;HTTP API / MCP 未建;change feed 端点未落(已知偏差) |
| §6.5.1 identifier 采集义务 | 未盘点(**未核实**),列入缺口 |
| §15.1 先轻后重 | ✓ 结构化 / 工具结果只留访问与追溯记录,raw 可选;职责不下放(§1) |
| §15.5 数据目录第一天建 | ✓ 检索键 / URI / views 已落;字典暴露与 feed 端点为缺口 |
| §15.6 baseline 暂不启用 | ✓ AShareForcast 仅登记为将来输入 |
| §15.7 不依赖实时在线 | ✓ 定时批 + 手动拉起;Lane C 也是批式受控回路 |
| §15.8 harness 无关 | ✓ 控制逻辑在 registry / catalog 配置与 action_log;字典 / 取数入口做成 CLI + 未来 MCP,均是包装不是依赖 |

## 6. 缺口清单(按路径;只列事实,不排程——排程由用户看定稿后决定)

```text
披露 PDF      G1 /v1/changes 端点 + observed/materialized 两语义;G2 envelope_kernel 反向接入
结构化数据    真实 WindRdsProvider / TushareApiProvider(含 freshness smoke、opt-in 真源测试);
              source_checkpoint 增量游标;字典两件(Lane B:契约件导出 + inventory 探测件);
              doctor 接通 F12 preflight(现仅环境诊断);ad-hoc 通道(Lane C:临时模板
              校验器、adhoc.* 命名空间、v1.2 增补条款);扩容候选核实(§3.6)
工具结果      source catalog 化;第一批真实源接入(候选与判据见附录 A)
人工上传      整个 upload adapter:登记 / 哈希 / OCR / 转写 / Excel 结构化 /
              material_type 人声明 / sensitivity / 分发三 kind
全域          HTTP API / MCP 包装;identifier 采集义务盘点(§4.4);
              两服务 change feed 端点与两事件语义
```

## 7. 开放问题(设计层,需用户拍板;时机类问题一律不列)

1. **upload adapter 形态**:独立服务 vs 并入 asset_intake 第三入口。kickoff 倾向独立服务;
   并入的收益是复用 registrar / 视图 / 权限底座,代价是服务职责变宽。
2. **Lane C 自由度**:三个档位——(i)AI 起草临时查询模板 + 确定性校验(§3.3 推荐形态,
   F7 / F8 兼容,v1.2 只需增补快速通道条款);(ii)受控自由 SELECT(自由度更高,但破 F7
   "只走白名单模板"与 F8"不是 text-to-SQL",需显式修订 v1.2 并论证与协议原则 11 的
   一致性,不推荐);(iii)仅现有白名单模板参数化(最保守,"自主取数"退化为只能组参)。
3. **通道终局确认**:wind 主 / tushare 备已是默认配置(F11);最终确认只是改或不改一行。

## 8. 风险清单

1. **供应商换轨 / 密钥轮换**(用户预告数月内可能全换):F4 / F11 / F12 已把换源收敛为
   改配置;轮换后必跑 doctor + preflight,缺表 / 缺列显式报错。
2. **凭证暂存于同事 skill 目录**(`docs/reference/api resource/`,已 gitignore):轮换前
   属临时态;轮换后凭证只留 shell / 私有 env,目录内 .env 删除。
3. **复权 as-of**:latest_recomputed 限制已写死(F3);正式回测级 qfq / hfq 在权益 PIT
   接入前禁用。
4. **字典维护成本**:字段 mapping / unit / fixture 只对 Lane A 强制;Lane B / C 靠
   "未实测"显式标注控风险——AI 用到未实测列时字典里能看出来。
5. **Lane C 误用**:临时模板校验器(F7 规则复用)与只读账号是护栏底线,启用前需实测
   (当前**未实测**);max_rows / timeout 防大拉;登记完整性靠 registrar 硬校验兜底。
6. **共享库测试卫生**:purge_test_rows 已落(74d3002);新集成测试沿用 `tests.` adapter
   前缀约定。
7. **并行会话同工作树**:显式路径提交纪律持续有效,直至 07 收尾。

## 附录 A. 轻载外部源调研事实(结论沿 v0.1;批次不排,选型判据见末尾)

同事包内已有(本机已读 SKILL.md):

- **news-newsapi-search**(NewsAPI.org):/everything + /top-headlines,5000+ 源,免费档
  100 请求/天。英文源为主(Reuters / BBC 级),对 A 股公司的中文本地新闻弱;适合海外
  供应链 / 行业信号;HTTP + key,接入成本低。
- **capture-search-x**(xAI Grok x_search):X/Twitter 实时搜索带引用。传闻 / 情绪类,
  按 §2.9 预设 Tier 3(status=cold),不能单独支撑正式预测;付费 key。
- 其余观察(未评):capture-alphapai-web、capture-gangtise-upgrade、capture-polymarket-trade
  ——纪要 / 预测市场类,与净利润预测主链的相关性待课题机制(§9)成型后再评。

公开候选(**全部未实测**,启用前逐个核权限 / 限流 / 条款):

- 论文类:arXiv API(免费)、OpenAlex(免费、元数据广)、Semantic Scholar(免费限流)。
  用途:技术趋势 → 行业主题课题;其消费方是拉式回路(evidence_request / 课题)——
  这是消费依赖事实,不是排程建议。
- 中文财经新闻:优先核 tushare 的 news / major_news 类接口(同一 token 同一通道,
  复用成本最低;权限**未实测**);财联社 / 东财等无官方开放 API,爬虫路径合规与稳定性差,
  不建议。
- 通用 web-search MCP(brave / tavily 类):可作 agent 侧工具,但结果进系统必须显式过
  register_tool_result;MCP server 本身按 F12 建 source catalog。

选型判据(供用户排批次时用):与净利润预测主链的相关性;locator 可得性(§4.7 准入底线,
拿不到可定位引用的源接了也进不了 L2);Tier 预设与置信度上限;接入成本(HTTP + key 最低);
条款与合规。
