# Minimal baseline for the current in-repo runner and runtime helpers.
PyYAML>=6.0
pandas>=2.2.0
pyarrow>=15.0.0

# Historical / future workspace packages retained as reference only.
# duckdb>=0.10.0
# torch>=2.2.0
# torchmetrics>=1.2.0
# matplotlib>=3.6.0
# pytest>=7.0.0
# pytest-mock>=3.10.0
# wandb>=0.17.0
